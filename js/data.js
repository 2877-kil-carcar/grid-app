export const GRID_SIZE = 15;

export let members = [];

export function setMembers(data) {
  members = data;
}

export let objects = [];

export function setObjects(newData) {
  objects = newData;
}